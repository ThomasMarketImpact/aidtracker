# Database connection (Neon PostgreSQL recommended)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# FTS API (no authentication required)
FTS_API_BASE=https://api.hpc.tools

# HAPI API
HAPI_API_BASE=https://hapi.humdata.org/api/v2
HAPI_APP_ID=your_base64_encoded_app_identifier
